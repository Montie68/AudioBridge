<Application x:Class="AudioBridgeUI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             ShutdownMode="OnExplicitShutdown">

    <Application.Resources>
        <!-- System tray context menu -->
        <ContextMenu x:Key="TrayContextMenu">
            <MenuItem Header="Open" Click="TrayOpen_Click" FontWeight="Bold"/>
            <MenuItem x:Name="TrayToggleBridgeItem" Header="Start Bridge" Click="TrayToggleBridge_Click"/>
            <Separator/>
            <MenuItem Header="Exit" Click="TrayExit_Click"/>
        </ContextMenu>

        <!-- System tray icon -->
        <tb:TaskbarIcon x:Key="TrayIcon"
                        IconSource="/Resources/tray_icon.ico"
                        ToolTipText="AudioBridge"
                        ContextMenu="{StaticResource TrayContextMenu}"
                        TrayLeftMouseDown="TrayIcon_TrayLeftMouseDown"/>
    </Application.Resources>
</Application>
